# Render 무료 배포 설정 (Blueprint).
# Render 대시보드에서 이 깃허브 레포를 연결하면 자동으로 읽어서 배포합니다.
services:
  - type: web
    name: class-trip-game
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
